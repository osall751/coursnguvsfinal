/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DicoPipe } from './dico.pipe';

describe('Pipe: Dicoe', () => {
  it('create an instance', () => {
    let pipe = new DicoPipe();
    expect(pipe).toBeTruthy();
  });
});
